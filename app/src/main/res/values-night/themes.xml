<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.IPSCollege" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Customize your dark theme here. -->

        <item name="colorPrimaryDark">@color/lavender</item>
        <item name="colorOnPrimary">@color/lavender</item>
        <!-- Secondary brand color. -->

        <!-- Status bar color. -->
        <item name="android:statusBarColor" >?attr/colorOnPrimary</item>
        <!-- Customize your theme.xml here. -->
    </style>

    <color name="text">@color/white</color>
    <color name="Primary">@color/textColor  </color>
    <color name="searchWhite">@color/black</color>
    <color name="searchBlack">@color/white</color>

    <style name="Theme.IPSCollege" parent="Base.Theme.IPSCollege" />
</resources>